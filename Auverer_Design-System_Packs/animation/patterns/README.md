# Pattern Registry

Suggested registry:

1. `reveal` — Motion
2. `stagger-list` — Motion
3. `hover-lift` — CSS/Motion
4. `page-transition` — Motion
5. `pinned-story` — GSAP + ScrollTrigger
6. `parallax-layers` — GSAP/ScrollTrigger
7. `product-travel` — GSAP/ScrollTrigger
8. `horizontal-scroll` — GSAP/ScrollTrigger
9. `image-mask-reveal` — Motion or GSAP
10. `smooth-scroll` — Lenis

Build project-specific implementations from these patterns instead of duplicating random snippets.
