# Universal Brand Kit Framework

This is a reusable framework, not a fixed brand.

Each project defines its own:
- colors
- typography
- spacing
- radius
- shadows
- motion tokens
- component states

Components consume semantic roles rather than raw colors.
